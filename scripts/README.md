# NS Browser scripts

This directory is reserved for reproducible patch/update helpers.

Do not silently replace Chromium's revision with an arbitrary number. Pin a real
commit/tag and record the Chromium revision in each release.
